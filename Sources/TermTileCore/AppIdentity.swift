/// Single source of truth for app naming — one name everywhere from commit 1.
public enum AppIdentity {
    public static let appName = "TermTile"
    public static let bundleID = "dev.ecn.apps.termtile"
}
