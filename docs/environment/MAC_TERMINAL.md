# Mac Terminal

Primary development happens on macOS.

Commands in this repo must be macOS-compatible or explicitly guarded. Do not assume GNU-only tools exist. Common traps include GNU timeout, GNU sed flags, Linux realpath behavior, apt-get, and systemctl.
