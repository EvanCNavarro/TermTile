- Redesigned the update experience: when a new version is available, TermTile now shows its own clean, branded dialog — app icon, a "What's new" list, and a clear Install button — instead of the plain system update alert
- Refreshed the About panel into a tidy identity card: app icon, version, a one-line description of what TermTile does, and quick GitHub + License links
- Polished the "Rearrange now" button — the icon and label now sit left-aligned with the keyboard shortcut tucked to the right, so it reads consistently with the rest of the menu
- Under the hood: TermTile now shares one design system with its companion app (RememBar), so the two look and feel consistent, and the update flow is covered by tests
