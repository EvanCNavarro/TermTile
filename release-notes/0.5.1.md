# TermTile 0.5.1

- Fixed a session waiting on a permission prompt showing as **finished**. Some prompts — asking to
  fetch a web page, for instance — put their cancel hint inside the decline option rather than in
  the footer, so TermTile did not recognise them as waiting on you. Those windows went green. They
  now go amber like every other prompt.
